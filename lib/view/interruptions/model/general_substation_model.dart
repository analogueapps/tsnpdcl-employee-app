class GeneralSubstationModel {
  final String name;
  final String rawData;  // Assuming you have raw data for each substation

  GeneralSubstationModel({required this.name, required this.rawData});
}